const { pool } = require("./server/db"); async function runMigration() { const client = await pool.connect(); try { const sql = require("fs").readFileSync("migrations/add_phone_number_to_contracts.sql", "utf8"); await client.query(sql); console.log("Migration completed successfully"); } catch (error) { console.error("Migration failed:", error); } finally { client.release(); pool.end(); } } runMigration();